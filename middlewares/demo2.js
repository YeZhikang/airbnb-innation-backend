const arr = [4,5,2,1,4]
const babelSort = (n) => {
    // n 代表最大的第n项
    for(let [index,val] of arr.entries()){
        for(let sid = index;sid<arr.length-1;sid++){
            if(val === sid)
        }
    }
}